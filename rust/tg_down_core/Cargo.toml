[package]
name = "tg_down_core"
version = "0.1.0"
edition = "2021"
description = "Pure Rust business rules for Tg-Down migration experiments"
license = "MIT"

[lib]
name = "tg_down_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
